# linkedin-job-hider

Chrome extension designed to remove "We won't show you this job again." listings from LinkedIn's job board page. Unfortunately LinkedIn will keep these listings in place, even though that quote hints that we no longer should see these once we remove them.
